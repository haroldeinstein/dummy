//= require jquery
//= require widgets/easy-audio.js

Audio = $('<audio src="<%= audio_path "click_down.mpg" %>">');
$('body').append(Audio);

$(document).ready(function() {
  $('.back').easyAudio({
    mpeg: '<%= audio_path "click_down.mp3" %>',
    wav: '<%= audio_path "click_down.wav" %>',
    onMouseDown: true
  });
});
